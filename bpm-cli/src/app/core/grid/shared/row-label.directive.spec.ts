import { RowLabelDirective } from './row-label.directive';

describe('RowLabelDirective', () => {
  it('should create an instance', () => {
    const directive = new RowLabelDirective();
    expect(directive).toBeTruthy();
  });
});
